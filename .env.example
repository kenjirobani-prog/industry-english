# Anthropic API key for Claude — used by /api/analyze-url and /api/analyze-file.
# Get one at https://console.anthropic.com
# Local:   set in .env.local (gitignored)
# Vercel:  vercel env add ANTHROPIC_API_KEY production
ANTHROPIC_API_KEY=
